import { DefaultTheme } from 'react-native-paper'

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: '#FFFFFF',
    primary: '#000000',
    secondary: '#FFFFFF',
    error: '#000000',
  },
}
